    <nav class="bg-gray-800 shadow-lg">
        <div class="max-w-7xl mx-auto px-2 sm:px-6 lg:px-8">
            <div class="relative flex items-center justify-between h-16">
                <!-- Logo or Text -->
                <div class="flex-shrink-0 flex items-center">
                    {{-- <a href="{{ route('index') }}" class="text-white font-bold text-xl">Your Logo or Text</a> --}}
                </div>
                <!-- Navigation Links -->
                <div class="hidden md:block">
                    <div class="ml-10 flex items-baseline space-x-4">
                        <!-- Navbar Links -->
                        {{-- <a href="{{ route('index') }}" class="text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium">Home</a> --}}
                        {{-- <a href="{{ route('login') }}" class="text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium">Login</a> --}}
                    </div>
                </div>
            </div>
        </div>
    </nav>